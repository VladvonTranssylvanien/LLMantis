# Agents specific
CLAUDE.md
AGENTS.md

# Never commit secrets
#
# `.env` and `.env.*` carry no slash, so they already match at any depth -
# lab/.env, deploy/.env and calibration/.env are covered by those two lines.
# What they do NOT cover is a secret file whose name merely ends in .env, and
# lab/.env.example names two of those as things people are likely to create.
# So: any *.env, plus the one named file it warns about.
.env
.env.*
!.env.example
*.env
lab/bots/keys.yaml
*.key
*.pem
credentials.json
secrets.yaml

# Local Compose overrides. Machine-specific by definition (a port remap, a
# different volume), and untracked is not the same as ignored: `git add -A`
# sweeps up whatever is lying around, and this repository is public.
docker-compose.override.yml

# Scan output: customer system prompts and full transcripts.
# These are customer trade secrets. They never reach version control.
results/
scans/
*.scan.json

# Python
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/
.mypy_cache/

# Database
*.db

# macOS
.DS_Store

# Editors
.vscode/
.idea/

# --- Brand source material: local only, never committed --------------------
#
# This repository is public. Everything below is licensed stock, or derived
# from it, and the licence forbids sub-licensing and inclusion in an online
# archive. The shipped mark is original geometry and depends on none of it.
#
# The app loads brand assets only from frontend/assets/brand/. Nothing in a
# build may reference Brand/ at all.
Brand/Screenshot*.png
Brand/mantis-transparent-background/
Brand/LLMantis/
Brand/*.zip

# tool output
art50-results.csv
