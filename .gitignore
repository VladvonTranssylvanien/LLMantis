# Agents specific
CLAUDE.md
AGENTS.md

# Never commit secrets
.env
.env.*
!.env.example
*.key
*.pem
credentials.json
secrets.yaml

# Scan output: customer system prompts and full transcripts.
# These are customer trade secrets. They never reach version control.
results/
scans/
*.scan.json

# Python
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/
.mypy_cache/

# Database
*.db

# macOS
.DS_Store

# Editors
.vscode/
.idea/

# --- Brand source material: local only, never committed --------------------
#
# This repository is public. Everything below is licensed stock, or derived
# from it, and the licence forbids sub-licensing and inclusion in an online
# archive. The shipped mark is original geometry and depends on none of it.
#
# The app loads brand assets only from frontend/assets/brand/. Nothing in a
# build may reference Brand/ at all.
Brand/Screenshot*.png
Brand/mantis-transparent-background/
Brand/LLMantis/
Brand/*.zip

# tool output
art50-results.csv
